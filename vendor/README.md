# Vendor staging

Generated third-party artifacts live here during local/release builds. The manifest,
not manually copied binaries, is the source of truth. Do not commit real artifacts
until repository and release policy are decided.

