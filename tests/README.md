# Tests

- `contracts/`: recorded tool output and parser contracts.
- `fixtures/`: tiny, redistributable media/image/PDF samples.
- `packaging/`: clean-machine installation and resource health checks.

